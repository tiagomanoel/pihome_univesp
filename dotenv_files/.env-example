SECRET_KEY="your-secret-key"  # You can generate a secret key using: python -c "import secrets; print(secrets.token_urlsafe(50))"

# 0 False, 1 True
DEBUG="1"

# Comma Separated values
ALLOWED_HOSTS="127.0.0.1,localhost"

DB_ENGINE="django.db.backends.postgresql"
POSTGRES_DB="your-database-name"
POSTGRES_USER="your-database-user"
POSTGRES_PASSWORD="your-database-password"
POSTGRES_HOST="your-database-host"
POSTGRES_PORT="5432"

# MQTT broker configurations
MQTT_HOST="your-mqtt-host"
MQTT_PORT="1883"
MQTT_CLIENT_NAME="your-client-name"
MQTT_KEEPALIVE="60"
MQTT_TOPIC="your-topic"

#Email configurations
EMAIL_BACKEND_ENV = 'django.core.mail.backends.smtp.EmailBackend'  
EMAIL_HOST_ENV = 'YOUR_SMTP_HOST' 
EMAIL_PORT_ENV = 'YOUR_SMTP_PORT'
EMAIL_USE_TLS_ENV = 'YOUR_SMTP_USE_TLS' #TRUE OR FALSE  
EMAIL_HOST_USER_ENV = 'YOUR_HOST_USER' 
EMAIL_HOST_PASSWORD_ENV = 'YOUR_HOST_PASSWORD'  
DEFAULT_FROM_EMAIL_ENV = 'DEFAULT_FROM_EMAIL' 